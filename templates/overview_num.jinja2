{% extends "overview.jinja2" %}

{% block content %}
  <h2>Numerologisch overzicht</h2>
<p>Het onderstaande overzicht toont de enkelvoudige getallen en de kaarten die daar bij horen.</p>

<table>
  <tr><td colspan=10 style="text-align: left"><h3>Grote arcana</h3></td></tr>
  <tr>
    {% for nr in ROMANS0 %}
      <td>{{nr}}: {{symbols.get(nr)}}</td>
    {% endfor %}
  </tr>
  <tr>
    {{render_cards(deck.cards, 0, 9)}}
  </tr>
  <tr>
    <td>&nbsp;</td>
    {{render_cards(deck.cards, 10, 18)}}
  </tr>
  <tr>
    <td>&nbsp;</td>
    {{render_cards(deck.cards, 19, 22)}}
  </tr>
  <tr><td colspan=10 style="text-align: left"><h3>Kleine arcana</h3></td></tr>
  <tr>
    <td>&nbsp;</td>
    {% for nr in ROMANS[:9] %}
      <td>{{nr}}: {{symbols.get(nr)}}</td>
    {% endfor %}
  </tr>
  <tr>
    <td rowspan=2>Staven: {{symbols.get('staven')}}</td>
    {{render_cards(deck.cards, 22, 31)}}
  </tr>
  <tr>
    {{render_cards(deck.cards, 31, 36)}}
  </tr>
  <tr>
    <td rowspan=2>Pentakels: {{symbols.get('pentakels')}}</td>
    {{render_cards(deck.cards, 36, 45)}}
  </tr>
  <tr>
    {{render_cards(deck.cards, 45, 50)}}
  </tr>
  <tr>
    <td rowspan=2>Zwaarden: {{symbols.get('zwaarden')}}</td>
    {{render_cards(deck.cards, 50, 59)}}
  </tr>
  <tr>
    {{render_cards(deck.cards, 59, 64)}}
  </tr>
  <tr>
    <td rowspan=2>Kelken: {{symbols.get('kelken')}}</td>
    {{render_cards(deck.cards, 64, 73)}}
  </tr>
  <tr>
    {{render_cards(deck.cards, 73, 78)}}
  </tr>
</table>

<p>Aanvullende informatie over numerologie:
  <ul>
    <li><a href="https://tarotstapvoorstap.nl/tarot-numerologie/">Tarot stap voor stap</a></li>
    <li><a href="http://www.kaartensterren.nl/pagina's/tarot%203%20getallen.html">Kaarten Sterren</a></li>
  </ul>
</p>

<p>Zie ook het <a href="{{url_for('overview', num=False)}}">algemene overzicht</a>.</p>
{% endblock content %}
