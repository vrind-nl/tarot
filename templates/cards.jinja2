{% extends "card.jinja2" %}


{% block content %}
    <h2>Kaartlegging</h2>
    <p>Hieronder zie {{cards | length}} willekeurige kaarten. 
    De toelichting is aanvankelijk verborgen, maar kan zichtbaar worden gemaakt met de knop "Toelichting" onder de kaarten.<p>
    {% if turned %}<p>Bij deze kaartlegging kunnen de kaarten omgedraaid zijn.
    Je kunt ze rechtop zetten met de knop "Rechtop".{% endif %}
    <table width=100%>
        <tr>
            {% for card in cards %}
                {% set coin = turned and range(0, 2) | random %}
                {% set turned = 'class="turned"' if coin else ''%}
                <td><img src="{{ url_for('static', filename=card.img) }}" {{turned}} height=400></td>
            {% endfor %}
        </tr>
        <tr><td colspan="{{cards | length}}"><button onclick="explanation()">Toelichting</button>
        {% if turned %}
            <button onclick="turn()">Rechtop</button>
        {% endif %}
        </td></tr>
        <tr>
            {% for card in cards %}
                <td>    
                    {{ card_info(card, deck.nr(card)) }}
                </td>
            {% endfor %}
        </tr>
    </table>
{% endblock %}